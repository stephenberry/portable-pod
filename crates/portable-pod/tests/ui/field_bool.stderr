error[E0277]: `bool` is not `Pod`, so its bytes are not portable across machines
 --> tests/ui/field_bool.rs:6:9
  |
6 |     on: bool,
  |         ^^^^ the trait `Pod` is not implemented for `bool`
  |
  = note: not valid for every bit pattern: `bool` (use `portable_pod::Bit`), `char`, `NonZero*`, enums, references
  = note: not position-independent: `usize`, `isize`, pointers, references, atomics, `Cell`/`RefCell` — use a fixed-width integer such as `u32` or `u64`
  = note: deliberately excluded: `f32`/`f64`, because NaN payloads are not stable across targets
  = note: for your own struct, add `#[derive(Pod)]` and `#[repr(C)]`
  = help: the following other types implement trait `Pod`:
            ()
            Bit
            Flags
            [T; N]
            i128
            i16
            i32
            i64
          and $N others

error[E0277]: `bool` is not `Pod`, so its bytes are not portable across machines
 --> tests/ui/field_bool.rs:5:8
  |
5 | struct Flags {
  |        ^^^^^ the trait `Pod` is not implemented for `bool`
  |
  = note: not valid for every bit pattern: `bool` (use `portable_pod::Bit`), `char`, `NonZero*`, enums, references
  = note: not position-independent: `usize`, `isize`, pointers, references, atomics, `Cell`/`RefCell` — use a fixed-width integer such as `u32` or `u64`
  = note: deliberately excluded: `f32`/`f64`, because NaN payloads are not stable across targets
  = note: for your own struct, add `#[derive(Pod)]` and `#[repr(C)]`
  = help: the following other types implement trait `Pod`:
            ()
            Bit
            Flags
            [T; N]
            i128
            i16
            i32
            i64
          and $N others
note: required for `Flags` to implement `Pod`
 --> tests/ui/field_bool.rs:5:8
  |
5 | struct Flags {
  |        ^^^^^
6 |     on: bool,
  |         ---- unsatisfied trait bound
  = help: consider manually implementing `Pod` to avoid undesired bounds
